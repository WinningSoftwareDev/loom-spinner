services:
  reverse-proxy:
    image: nginx:stable
    container_name: loom-spinner-reverse-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./conf.d:/etc/nginx/conf.d:cached
      - ./certs:/etc/nginx/certs
      - ./html/502.html:/usr/share/nginx/html/502.html:ro
    networks:
      - spinner-net

networks:
  spinner-net:
    name: loom-spinner-network
    driver: bridge
